package arrays;

public class SortedMatrix {

    public int getElementSortedMatrix(int[][] a, int element) {
        // as the matrix is sorted
        // first observation is if the element is smaller then the last column,
        // we move to the left
        // if the element is smaller than the first row, no solution
        return 0;
    }

}
